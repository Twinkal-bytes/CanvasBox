import { Button } from "@mui/material";
import React from "react";

const CreateCanvas =(handleInput)=>{
    return(
        <>
        {/* <Button onClick={handleInput}>Create Box</Button> */}
        </>
    )
}

export default CreateCanvas